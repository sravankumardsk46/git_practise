############git branches#############

git branch branchname     { create}

git branch -a   to shoe origin remotmain /local main

git checkout    to chenge the branch 


git checkout -b branchname      to create new branch and go there 

git switch        to chenge branch to main


git pull : it going to check any code avilable in remote repository so pull it and merge also

git fetch : it is going to check any chenges in remote repocan pull it but not merge

git pull = git fetch + git merge



@############ REJECTED ########
frist u created manuvalli one file origin main then com back to 
to go to local machin create one touch file then add and commit it push but it REJECTED becouse nedd PULL


git pull ......it is not woking

git fetch  { it can take that chenges only}
git merge   { it can downlode that chenges to our local machine }
now REJECTED working git push

#########################git stash and git stash apply ##############

touch hide
git add . then only traced we will hid file
git stash  file will hide
git stash apply  file will shows

################## HEAD SOFT /HARD #################
touch softheadfile
git add .
git commit -m " added"
#### so now check
git log --oneline
git reset --soft HEAD~1     this latest commit id is gone working and staging area undo
git reset --hard HEAD~1      this latest comit id never comeback undu from working dir and staging area

######################gitignore #########################
 
